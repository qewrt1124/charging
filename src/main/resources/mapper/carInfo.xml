<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.car">
  <!-- 전기차 정보 넣기 -->
  <insert id="carInfo" parameterType="project.oecr.dto.CarInfoDto">
    insert into carinfo
    values (#{manuFac}, #{model}, #{batCap}, #{outPut})
  </insert>
</mapper>