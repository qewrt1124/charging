<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.api">
  <!-- 전기차 데이터 넣기 -->
  <insert id="carInfo" parameterType="project.oecr.dto.CarInfoDto">
    insert into carInfo
    values (null, #{cId}, #{manuFac}, #{model}, #{batCap});
  </insert>
</mapper>